server:
  port: 8084

spring:
  application:
    name: admin-service

  # Feign HTTP client (HC5) is enabled via dependency + config class,
  # no extra properties needed here.

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
